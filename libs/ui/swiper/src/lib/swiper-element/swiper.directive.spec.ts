import {SwiperElementDirective} from "./swiper-element.directive";

describe('SwiperDirective', () => {
  it('should create an instance', () => {
    const directive = new SwiperElementDirective();
    expect(directive).toBeTruthy();
  });
});
